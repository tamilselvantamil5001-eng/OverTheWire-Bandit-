#level 7

#To login for the next level we want change the directory has bandit6 - bandit7.
ssh bandit7@bandit.labs.overthewire.org
#password : "morbNTDkSW6jIlUc0ymOdMaLnOlFVAaj"
HWasnPhtq9AVKe0dmk45nxy20cvUa6EG

#For the next level password access is stored in the file data.txt next to the word millionth.
#In this level first we want to find how many words present file data.txt,because in this they meanshion that the password is next to the word millionth.
#So we use the commond "cat data.txt | wc -l.It shows the number words
#Now using the grep coomond to find the perticular word "Millionth" so cat data.txt| grep "millionth".
#Finally we get the password for the next level is "dfwvzFQi4mU0wfNbFOe9RoWskMLg7eEc".

#Now exit from the level 7...
